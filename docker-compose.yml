version: "3.9"

services:
  mysql:
    image: mysql:8
    container_name: mysql_db
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: MMDb
    ports:
      - "5400:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: